server {
    server_name  _;

    root /usr/share/nginx/html;

    location /players {
        default_type text/html;
    }

    location / {
        try_files $uri @engine;
    }

    location @engine {
        proxy_pass http://engine:22936;
    }
}
